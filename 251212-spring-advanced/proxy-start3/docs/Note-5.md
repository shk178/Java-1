1. 스프링 AOP에서 포인트컷 지시자(PCD)는 주로 무엇을 정의할까요?

A
어드바이스의 종류

B
애스펙트 실행 순서

조인 포인트 대상 범위

D
트랜잭션 격리 레벨



해설
포인트컷 지시자는 어디에 어드바이스를 적용할지, 즉 조인 포인트의 대상을 '필터링'하여 범위를 정하는 역할을 한답니다.
2. `execution` 포인트컷 지시자는 어떤 시점을 조인 포인트로 매칭할까요?

A
필드 접근

B
생성자 호출

메서드 실행

D
예외 발생



해설
execution은 이름 그대로 메서드가 실행되는 시점을 포착하는 가장 기본적인 포인트컷 지시자예요. 다른 지시자들과 구분된답니다.
3. `execution` 포인트컷 표현식에서 매개변수 패턴 `(...)`는 무엇을 의미할까요?

A
매개변수 없음

B
임의의 1개 매개변수

0개 이상의 임의 매개변수

D
정확히 3개의 임의 매개변수



해설
execution에서 '...'은 매개변수의 타입과 개수에 상관없이 0개 이상 어떤 매개변수든 허용한다는 와일드카드랍니다. '*'과는 다르죠.
4. Spring AOP에서 `this`와 `target` 포인트컷 지시자의 가장 큰 차이점은 무엇일까요?

A
this는 실제 객체, target은 프록시

this는 프록시, target은 실제 객체

C
this는 메서드, target은 클래스

D
this는 인터페이스, target은 구현체



해설
Spring AOP는 프록시 기반이라 this는 스프링 컨테이너에 등록된 '프록시' 객체를 가리켜요. target은 프록시가 실제 호출하는 '원본' 객체랍니다.
5. `execution` 지시자를 사용할 때, 포인트컷 표현식의 선언 타입(Declaration Type)으로 인터페이스와 같은 부모 타입을 지정하면 어떻게 될까요?

A
해당 부모 타입의 메서드만 매칭됩니다.

B
자식 타입에 정의된 모든 메서드가 매칭됩니다.

부모 타입에 선언된 메서드를 구현한 자식 타입의 메서드까지 매칭됩니다.

D
부모 타입 자체는 매칭되지 않습니다.



해설
execution은 선언 타입으로 부모 타입을 지정해도 다형성(polymorphism)이 적용되어, 해당 부모 인터페이스/클래스에 정의된 메서드를 구현한 자식의 메서드까지 매칭한답니다.
6. `within` 포인트컷 지시자의 주요 제약사항은 무엇일까요?

A
메서드 이름을 사용할 수 없음

부모 타입/인터페이스를 지정할 수 없음

C
매개변수 타입을 지정할 수 없음

D
반환 타입을 지정할 수 없음



해설
within은 특정 '타입 내부'에 있는 조인 포인트를 매칭하지만, execution과 달리 부모 타입이나 인터페이스로는 매칭할 수 없고 정확한 구현체 타입만 지정해야 한답니다.
7. `args` 포인트컷 지시자와 `execution`의 매개변수 매칭 방식의 주요 차이점은 무엇일까요?

A
args는 시그니처 기반, execution은 런타임 객체 기반

args는 런타임 객체 기반, execution은 시그니처 기반

C
args는 부모 타입 미허용, execution은 부모 타입 허용

D
args는 어노테이션만 매칭



해설
args는 메서드 호출 시 전달되는 '실제 객체'의 런타임 타입을 보고 동적으로 매칭해요. 반면 execution은 '메서드 시그니처'의 선언된 타입만 보고 정적으로 매칭한답니다.
8. `@annotation` 포인트컷 지시자는 무엇을 대상으로 조인 포인트를 매칭할까요?

A
클래스 어노테이션

메서드 어노테이션

C
매개변수 어노테이션

D
필드 어노테이션



해설
@annotation 지시자는 이름에서 알 수 있듯, 조인 포인트가 되는 '메서드 자체'에 특정 어노테이션이 붙어있는 경우를 매칭할 때 사용된답니다.
9. 클래스에 특정 어노테이션이 적용되었을 때, `@target`과 `@within` 포인트컷 지시자의 적용 범위 차이는 무엇일까요?

A
@target은 해당 클래스, @within은 부모 클래스까지

@target은 부모 포함 모든 메서드, @within은 해당 클래스 메서드만

C
@target은 해당 클래스 메서드만, @within은 부모 포함 모든 메서드

D
@target은 필드, @within은 메서드



해설
@target은 어노테이션이 붙은 '객체 인스턴스'의 모든 메서드(부모 메서드 포함)를 매칭하지만, @within은 해당 어노테이션이 붙은 '타입 정의 내부'의 메서드만 매칭한답니다.
10. `bean` 포인트컷 지시자는 무엇을 대상으로 하며, 어떤 환경에서 사용될까요?

A
클래스 이름, AspectJ

스프링 빈 이름, Spring

C
메서드 시그니처, AspectJ

D
패키지 구조, Spring



해설
bean 지시자는 Spring Framework에 특화된 것으로, AspectJ 표준에는 없어요. 스프링 컨테이너에 등록된 '빈의 이름'을 기준으로 조인 포인트를 지정한답니다.
