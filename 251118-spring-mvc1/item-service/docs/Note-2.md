1. 스프링 MVC로 동적 웹 페이지를 만들 때, 기본 웹 기능과 HTML 템플릿 처리를 위해 필요한 주요 의존성 조합은 무엇일까요?

A
Web, H2

Web, Thymeleaf

C
JPA, Lombok

D
Test, Web



해설
동적 웹 페이지를 만들려면 웹 기능과 템플릿 엔진이 필요해요. Spring Boot Web 의존성은 웹 기능을, Thymeleaf 의존성은 서버에서 HTML을 동적으로 만들어줘요.
2. MVC 패턴에서 사용자의 요청을 처음 받아 처리 방향을 결정하는 중심적인 역할은 무엇인가요?

A
View

B
Model

C
Repository

Controller



해설
MVC에서 컨트롤러는 클라이언트 요청을 받아 적절한 서비스나 로직을 호출하고, 그 결과를 모델에 담아 뷰로 전달하는 중심 역할을 해요.
3. 애플리케이션 내에서 데이터를 저장소에 넣거나 가져오는 등의 데이터 접근 로직을 추상화하여 담당하는 역할은 무엇일까요?

A
Controller

B
View

C
Service

Repository



해설
레포지토리는 데이터 저장소(DB 등)에 접근하는 로직을 추상화하여, 데이터 관리와 관련된 일관된 인터페이스를 제공하는 역할을 맡아요.
4. 타임리프(Thymeleaf)의 '내추럴 템플릿(Natural Template)'이라고 부르는 이유가 무엇일까요?

순수 HTML 파일을 브라우저에서 바로 열어 레이아웃을 확인할 수 있기 때문

B
복잡한 설정 없이도 동적 기능을 자동으로 구현해주기 때문

C
데이터베이스 연결 및 쿼리를 자동으로 생성해주기 때문

D
다른 템플릿 엔진보다 코드가 간결하고 가독성이 좋기 때문



해설
타임리프의 가장 큰 특징 중 하나는 순수 HTML 구조를 유지하면서 동적인 요소를 추가할 수 있어, 웹 서버 없이도 브라우저에서 레이아웃을 확인할 수 있다는 점이에요.
5. 컨트롤러가 모델에 담아 뷰로 전달한 데이터의 특정 값을 타임리프 템플릿에서 사용하려면 어떤 표현식을 쓸까요?

A
#{}

B
@{}

${}

D
*{}



해설
`${...}` 형태의 변수 표현식은 타임리프에서 모델에 담긴 객체나 데이터의 속성 값에 접근하고 출력할 때 사용되는 기본적인 문법이에요.
6. 다중 스레드 환경에서 여러 곳에서 동시에 접근하여 데이터를 수정할 수 있는 환경에서 안전하게 사용하기 어려운 인메모리 저장소 구현체는 무엇인가요?

A
ConcurrentHashMap

B
ArrayList

HashMap

D
Array



해설
`HashMap`은 여러 스레드가 동시에 접근하여 데이터를 변경할 때 예상치 못한 결과나 오류를 발생시킬 수 있어요. 안전하게 사용하려면 `ConcurrentHashMap` 같은 자료구조를 써야 합니다.
7. 스프링 MVC의 `@ModelAttribute`는 HTTP 요청 파라미터를 자바 객체로 자동 바인딩해주는 편리한 기능을 제공하는데요, 이 기능의 주요 이점은 무엇일까요?

A
복잡한 유효성 검사 로직 자동 적용

폼 데이터 필드와 객체 속성의 자동 연결

C
HTTP 응답 본문 자동 생성

D
웹 서버 재시작 없이 코드 변경 반영



해설
`@ModelAttribute`는 HTTP 요청 파라미터를 분석하여 지정된 객체의 속성에 자동으로 값을 채워주는 기능을 해요. 이를 통해 컨트롤러에서 폼 데이터를 객체로 쉽게 다룰 수 있어요.
8. 표준 HTML `<form>` 태그의 `method` 속성에 직접 지정하여 데이터를 전송할 수 있는 HTTP 메서드는 보통 무엇인가요?

A
GET, PUT

B
POST, DELETE

GET, POST

D
PUT, DELETE



해설
표준 HTML 폼 태그는 데이터 전송을 위해 GET 방식과 POST 방식만을 기본적으로 지원합니다. PUT이나 DELETE 등 다른 HTTP 메서드는 직접 사용할 수 없어요.
9. 웹 페이지에서 폼 데이터를 POST로 제출한 후, 브라우저 새로고침 시 동일한 데이터가 다시 제출되어 중복 생성을 유발하는 문제를 방지하기 위한 디자인 패턴은 무엇인가요?

A
MVC 패턴

B
DI 패턴

PRG 패턴

D
DAO 패턴



해설
PRG 패턴은 POST 요청 처리 후 GET 요청으로 리다이렉트하여, 새로고침 시 마지막 POST 요청이 아닌 GET 요청이 다시 전송되게 함으로써 중복 저장을 방지하는 방법이에요.
10. 스프링 MVC에서 POST 요청 처리 후 리다이렉트할 때, 리다이렉트된 페이지에 성공 메시지 같은 일회성 데이터를 함께 전달하고 싶을 때 사용하는 기능은 무엇일까요?

A
Model 객체

B
HttpSession

RedirectAttributes

D
HttpServletRequest



해설
`RedirectAttributes`는 리다이렉트 시 URL 경로 변수에 값을 추가하거나, 쿼리 파라미터 형태로 데이터를 자동으로 인코딩하여 다음 페이지에 전달할 수 있게 도와줘요.